<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bocanegra.mauro.yabank">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeFullScreen">
        <activity android:name=".presentationlayer.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="@string/app_name_lower"
                    android:scheme="example" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentationlayer.activities.LoginActivity"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".presentationlayer.activities.MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".presentationlayer.activities.RecargaActivity"
            android:theme="@style/AppThemeNoBar">
        </activity>
    </application>

</manifest>